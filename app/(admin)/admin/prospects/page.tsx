import { Placeholder } from '@/components/ui/Placeholder';
import { stubProspect } from '@/lib/stubs';

export default function ProspectsPage() {
  return (
    <Placeholder
      name="Route: /admin/prospects (Phase 6 — qualification scorecard)"
      props={{ example: stubProspect }}
    />
  );
}
