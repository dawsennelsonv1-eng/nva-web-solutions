import { Placeholder } from '@/components/ui/Placeholder';

export default function CombinerPage() {
  return (
    <Placeholder
      name="Route: /admin/combiner (Phase 9 — dnd-kit, touch + non-drag fallback)"
      props={{}}
    />
  );
}
